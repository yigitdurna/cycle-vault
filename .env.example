# Copy to .env (which is gitignored) and fill in, OR source your secrets file
# before running dev scripts. Never commit a real key.

# Used by scripts/openai-review.mjs for an independent code review.
OPENAI_API_KEY=
# Optional model override (defaults to gpt-5.5)
# OPENAI_MODEL=gpt-5.5
